import type { TableStructure } from "@/Share/utils/tableStructure";

export const MONSTER_STRUCTURE: TableStructure = {
    "datastructure": [
        {
            "prop": "id",
            "columnName": "monsters.id",
            "width": 10
        },
        {
            "prop": "name",
            "columnName": "monsters.name",
            "width": 60
        },
    ]
}